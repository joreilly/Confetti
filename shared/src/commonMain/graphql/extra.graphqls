extend schema @link(url: "https://specs.apollo.dev/kotlin_labs/v0.2", import: ["@typePolicy"])

extend type Query @typePolicy(connectionFields: "sessions")

extend type Session @typePolicy(keyFields: "id")
